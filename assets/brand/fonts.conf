<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<!-- Only the bundled fonts, so the icon's digits render in Outfit (#54). -->
<fontconfig>
  <dir prefix="cwd">assets/fonts</dir>
  <cachedir prefix="cwd">build/fontconfig-cache</cachedir>
</fontconfig>
