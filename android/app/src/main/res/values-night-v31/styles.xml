<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+: the system splash shows the launcher icon on navy, then the
     same navy until Flutter's first frame (#54). -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_navy</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/splash_navy</item>
        <item name="android:windowSplashScreenBackground">@color/splash_navy</item>
    </style>
</resources>
